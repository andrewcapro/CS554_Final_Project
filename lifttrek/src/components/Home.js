import React from 'react'

function Home() {
  return (
    <div>
      Welcome to LiftTrek
      {/* <button onClick={() => app.auth().signOut()}>Sign Out</button> */}
    </div>
  )
}

export default Home
